server {
    listen 80;
    server_name env-95b504d4.localhost;

    location / {
        proxy_pass http://app-env-95b504d4:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
